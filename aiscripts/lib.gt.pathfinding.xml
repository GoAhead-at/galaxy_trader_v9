<?xml version="1.0" encoding="utf-8"?>
<aiscript name="lib.gt.pathfinding" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="../../stock_game/X4 Foundations/libraries/aiscripts.xsd" version="2">
  <params>
    <param name="mode" default="'spaces'"/>
    <param name="ship" default="this.ship"/>
    <param name="homeSector" default="this.ship.sector"/>
    <param name="maxDistance" default="0"/>
    <param name="maxDistanceBuy" default="0" comment="Optional supplier-leg radius. 0 = use maxDistance. When set with maxDistanceSell, BuySpaces/SellSpaces are split."/>
    <param name="maxDistanceSell" default="0" comment="Optional delivery-leg radius. 0 = use maxDistance. When set with maxDistanceBuy, BuySpaces/SellSpaces are split."/>
    <param name="blacklistGroup" default="null"/>
    <param name="ignoreBlacklist" default="false" comment="spaces mode: neutral home-range sector list (cache fill). BL enforced at cache read via lib.gt.cache.score_for_ship."/>
    <param name="sectorWhitelist" default="[]" comment="spaces mode: GT order whitelist. Blacklisted sectors listed here stay in scope."/>
    <param name="filterBlacklistedSpaces" default="false" comment="spaces mode: drop sectors that are themselves blacklisted, without requiring a blacklist-aware path from home. Keeps home-disconnected but allowed sectors in scope."/>
    <param name="allowIllegal" default="false"/>
    <param name="enforceRouteIllegalLegality" default="false"/>
    <param name="startSector" default="null"/>
    <param name="buySector" default="null"/>
    <param name="sellSector" default="null"/>
    <param name="ware" default="null"/>
    <param name="checkSector" default="null"/>
    <param name="routeCacheTtl" default="300s"/>
    <param name="pilotSkill" default="null"/>
    <param name="includeCommander" default="false"/>
    <param name="debugchance" default="0"/>
  </params>
  <attention min="unknown">
    <actions>
      <!-- ================================================================= -->
      <!-- MODE: effective_pilot_level                                        -->
      <!-- ================================================================= -->
      <do_if value="$mode == 'effective_pilot_level'">
        <set_value name="$resolvedLevel" exact="null"/>
        <set_value name="$useVanillaXPOnly" exact="global.$GT_GlobalSettings? and global.$GT_GlobalSettings.$XP? and global.$GT_GlobalSettings.$XP.$UseVanillaXPOnly? and global.$GT_GlobalSettings.$XP.$UseVanillaXPOnly"/>

        <do_if value="$ship? and $ship.pilot?">
          <do_if value="$useVanillaXPOnly">
            <do_if value="$ship.pilot.skill.piloting?">
              <set_value name="$resolvedLevel" exact="$ship.pilot.skill.piloting"/>
            </do_if>
          </do_if>
          <do_elseif value="global.$GT_Pilots? and global.$GT_Pilots.{$ship.pilot}? and global.$GT_Pilots.{$ship.pilot}.$Level?">
            <set_value name="$resolvedLevel" exact="global.$GT_Pilots.{$ship.pilot}.$Level"/>
          </do_elseif>
        </do_if>

        <do_if value="$includeCommander and $ship? and $ship.commander? and $ship.commander.pilot?">
          <set_value name="$commanderLevel" exact="null"/>
          <do_if value="$useVanillaXPOnly">
            <do_if value="$ship.commander.pilot.skill.piloting?">
              <set_value name="$commanderLevel" exact="$ship.commander.pilot.skill.piloting"/>
            </do_if>
          </do_if>
          <do_elseif value="global.$GT_Pilots? and global.$GT_Pilots.{$ship.commander.pilot}? and global.$GT_Pilots.{$ship.commander.pilot}.$Level?">
            <set_value name="$commanderLevel" exact="global.$GT_Pilots.{$ship.commander.pilot}.$Level"/>
          </do_elseif>

          <do_if value="$commanderLevel != null">
            <do_if value="$resolvedLevel == null">
              <set_value name="$resolvedLevel" exact="$commanderLevel"/>
            </do_if>
            <do_else>
              <set_value name="$resolvedLevel" exact="[$resolvedLevel, $commanderLevel].max"/>
            </do_else>
          </do_if>
        </do_if>

        <wait exact="1ms" comment="Cooperative yield before return (wake-loop guard)"/>
        <return value="$resolvedLevel"/>
      </do_if>

      <!-- ================================================================= -->
      <!-- MODE: pilot_max_jumps                                              -->
      <!-- ================================================================= -->
      <do_if value="$mode == 'pilot_max_jumps'">
        <set_value name="$maxJumps" exact="null"/>
        <do_if value="$pilotSkill != null">
          <do_if value="$pilotSkill le 2">
            <set_value name="$maxJumps" exact="1"/>
          </do_if>
          <do_elseif value="$pilotSkill le 5">
            <set_value name="$maxJumps" exact="3"/>
          </do_elseif>
          <do_elseif value="$pilotSkill le 8">
            <set_value name="$maxJumps" exact="5"/>
          </do_elseif>
          <do_elseif value="$pilotSkill le 11">
            <set_value name="$maxJumps" exact="10"/>
          </do_elseif>
          <do_elseif value="$pilotSkill le 13">
            <set_value name="$maxJumps" exact="15"/>
          </do_elseif>
          <do_else>
            <set_value name="$maxJumps" exact="25"/>
          </do_else>
        </do_if>
        <wait exact="1ms" comment="Cooperative yield before return (wake-loop guard)"/>
        <return value="$maxJumps"/>
      </do_if>

      <!-- ================================================================= -->
      <!-- MODE: sector_cargo_legality                                       -->
      <!-- Check whether Monitored contraband may enter $checkSector.        -->
      <!-- ================================================================= -->
      <do_if value="$mode == 'sector_cargo_legality'">
        <set_value name="$result" exact="table[
          $IsLegal = true,
          $Reason = 'skipped',
          $BlockedWare = null,
          $BlockedPoliceFaction = null,
          $ShouldEnforce = false
        ]"/>
        <do_if value="not $allowIllegal or not $enforceRouteIllegalLegality">
          <wait exact="1ms" comment="Cooperative yield before return (wake-loop guard)"/>
          <return value="$result"/>
        </do_if>
        <do_if value="$ship == null or $checkSector == null">
          <set_value name="$result.$IsLegal" exact="false"/>
          <set_value name="$result.$Reason" exact="'invalid_context'"/>
          <set_value name="$result.$ShouldEnforce" exact="true"/>
          <wait exact="1ms" comment="Cooperative yield before return (wake-loop guard)"/>
          <return value="$result"/>
        </do_if>
        <set_value name="$result.$ShouldEnforce" exact="true"/>
        <set_value name="$shipOwner" exact="$ship.owner"/>
        <do_if value="$shipOwner == null">
          <set_value name="$result.$IsLegal" exact="false"/>
          <set_value name="$result.$Reason" exact="'invalid_owner'"/>
          <wait exact="1ms" comment="Cooperative yield before return (wake-loop guard)"/>
          <return value="$result"/>
        </do_if>
        <do_if value="not $checkSector.policefaction or ($checkSector.policefaction == $shipOwner)">
          <set_value name="$result.$Reason" exact="'no_police'"/>
          <wait exact="1ms" comment="Cooperative yield before return (wake-loop guard)"/>
          <return value="$result"/>
        </do_if>
        <set_value name="$cargoList" exact="$ship.cargo.list"/>
        <do_if value="$cargoList.count == 0">
          <set_value name="$result.$Reason" exact="'no_cargo'"/>
          <wait exact="1ms" comment="Cooperative yield before return (wake-loop guard)"/>
          <return value="$result"/>
        </do_if>
        <do_all exact="$cargoList.count" counter="$ci">
          <set_value name="$cargoWare" exact="$cargoList.{$ci}"/>
          <do_if value="$cargoWare? and @$cargoWare.illegal gt 0 and $cargoWare.illegalto.{$checkSector.policefaction}.{$shipOwner}">
            <set_value name="$result.$IsLegal" exact="false"/>
            <set_value name="$result.$Reason" exact="'illegal_on_route'"/>
            <set_value name="$result.$BlockedWare" exact="$cargoWare"/>
            <set_value name="$result.$BlockedPoliceFaction" exact="$checkSector.policefaction"/>
            <break/>
          </do_if>
        </do_all>
        <wait exact="1ms" comment="Cooperative yield before return (wake-loop guard)"/>
        <return value="$result"/>
      </do_if>

      <!-- ================================================================= -->
      <!-- MODE: invalidate_route_legality_cache                             -->
      <!-- Clear cached route_legality verdict for one trade tuple (R3).     -->
      <!-- ================================================================= -->
      <do_if value="$mode == 'invalidate_route_legality_cache'">
        <set_value name="$result" exact="table[$Invalidated = false]"/>
        <do_if value="$homeSector? and $buySector? and $sellSector? and $ware? and $ship? and $ship.owner?">
          <set_value name="$cacheOwner" exact="$ship.owner"/>
          <set_value name="$cacheBlacklist" exact="$blacklistGroup"/>
          <do_if value="$cacheBlacklist == null">
            <set_value name="$cacheBlacklist" exact="blacklistgroup.civilian"/>
          </do_if>
          <set_value name="$cacheStart" exact="$startSector"/>
          <do_if value="$cacheStart == null">
            <set_value name="$cacheStart" exact="if $ship.sector? then $ship.sector else $buySector"/>
          </do_if>
          <do_if value="global.$GT_RouteLegalityCache? and global.$GT_RouteLegalityCache.{$homeSector}? and global.$GT_RouteLegalityCache.{$homeSector}.{$buySector}? and global.$GT_RouteLegalityCache.{$homeSector}.{$buySector}.{$sellSector}? and global.$GT_RouteLegalityCache.{$homeSector}.{$buySector}.{$sellSector}.{$ware}? and global.$GT_RouteLegalityCache.{$homeSector}.{$buySector}.{$sellSector}.{$ware}.{$allowIllegal}? and global.$GT_RouteLegalityCache.{$homeSector}.{$buySector}.{$sellSector}.{$ware}.{$allowIllegal}.{$enforceRouteIllegalLegality}? and global.$GT_RouteLegalityCache.{$homeSector}.{$buySector}.{$sellSector}.{$ware}.{$allowIllegal}.{$enforceRouteIllegalLegality}.{$cacheOwner}? and global.$GT_RouteLegalityCache.{$homeSector}.{$buySector}.{$sellSector}.{$ware}.{$allowIllegal}.{$enforceRouteIllegalLegality}.{$cacheOwner}.{$cacheBlacklist}? and global.$GT_RouteLegalityCache.{$homeSector}.{$buySector}.{$sellSector}.{$ware}.{$allowIllegal}.{$enforceRouteIllegalLegality}.{$cacheOwner}.{$cacheBlacklist}.{$cacheStart}?">
            <set_value name="global.$GT_RouteLegalityCache.{$homeSector}.{$buySector}.{$sellSector}.{$ware}.{$allowIllegal}.{$enforceRouteIllegalLegality}.{$cacheOwner}.{$cacheBlacklist}.{$cacheStart}" exact="[]"/>
            <set_value name="$result.$Invalidated" exact="true"/>
          </do_if>
        </do_if>
        <wait exact="1ms" comment="Cooperative yield before return (wake-loop guard)"/>
        <return value="$result"/>
      </do_if>

      <!-- ================================================================= -->
      <!-- MODE: route_enforce_precheck                                        -->
      <!-- Pre-transit guard: read ActiveTradePolicy, check next sector.       -->
      <!--                                                                     -->
      <!-- UNREACHABLE - no caller. The live guard is inlined twice in         -->
      <!-- move.generic.xml (capital gate path + non-capital move_to path)     -->
      <!-- because X4 binds result= to the next run_script, so this mode       -->
      <!-- cannot be called from there. Kept as the readable reference for     -->
      <!-- the abort contract; change move.generic when changing behaviour,    -->
      <!-- and do not assume this copy is in sync.                             -->
      <!-- ================================================================= -->
      <do_if value="$mode == 'route_enforce_precheck'">
        <set_value name="$result" exact="table[
          $ShouldAbort = false,
          $IsLegal = true,
          $Reason = 'skipped',
          $BlockedWare = null,
          $BlockedPoliceFaction = null,
          $CacheInvalidated = false
        ]"/>
        <do_if value="$ship == null or $checkSector == null">
          <wait exact="1ms" comment="Cooperative yield before return (wake-loop guard)"/>
          <return value="$result"/>
        </do_if>
        <do_if value="not global.$GT_Ships? or not global.$GT_Ships.{$ship}? or not global.$GT_Ships.{$ship}.$ActiveTradePolicy?">
          <wait exact="1ms" comment="Cooperative yield before return (wake-loop guard)"/>
          <return value="$result"/>
        </do_if>
        <set_value name="$policy" exact="global.$GT_Ships.{$ship}.$ActiveTradePolicy"/>
        <do_if value="not $policy.$AllowIllegal? or not $policy.$AllowIllegal or not $policy.$EnforceRouteIllegalLegality? or not $policy.$EnforceRouteIllegalLegality">
          <wait exact="1ms" comment="Cooperative yield before return (wake-loop guard)"/>
          <return value="$result"/>
        </do_if>
        <!-- Inline sector cargo legality (avoid recursive lib.gt.pathfinding run_script; breaks caller result= binding). -->
        <set_value name="$legality" exact="table[
          $IsLegal = true,
          $Reason = 'skipped',
          $BlockedWare = null,
          $BlockedPoliceFaction = null
        ]"/>
        <set_value name="$shipOwner" exact="$ship.owner"/>
        <do_if value="$shipOwner == null">
          <set_value name="$legality.$IsLegal" exact="false"/>
          <set_value name="$legality.$Reason" exact="'invalid_owner'"/>
        </do_if>
        <do_elseif value="not $checkSector.policefaction or ($checkSector.policefaction == $shipOwner)">
          <set_value name="$legality.$Reason" exact="'no_police'"/>
        </do_elseif>
        <do_else>
          <set_value name="$cargoList" exact="$ship.cargo.list"/>
          <do_if value="$cargoList.count == 0">
            <set_value name="$legality.$Reason" exact="'no_cargo'"/>
          </do_if>
          <do_else>
            <do_all exact="$cargoList.count" counter="$ci">
              <set_value name="$cargoWare" exact="$cargoList.{$ci}"/>
              <do_if value="$cargoWare? and @$cargoWare.illegal gt 0 and $cargoWare.illegalto.{$checkSector.policefaction}.{$shipOwner}">
                <set_value name="$legality.$IsLegal" exact="false"/>
                <set_value name="$legality.$Reason" exact="'illegal_on_route'"/>
                <set_value name="$legality.$BlockedWare" exact="$cargoWare"/>
                <set_value name="$legality.$BlockedPoliceFaction" exact="$checkSector.policefaction"/>
                <break/>
              </do_if>
            </do_all>
          </do_else>
        </do_else>
        <do_if value="$legality.$IsLegal">
          <wait exact="1ms" comment="Cooperative yield before return (wake-loop guard)"/>
          <return value="$result"/>
        </do_if>
        <set_value name="$result.$IsLegal" exact="false"/>
        <set_value name="$result.$Reason" exact="$legality.$Reason"/>
        <set_value name="$result.$BlockedWare" exact="$legality.$BlockedWare"/>
        <set_value name="$result.$BlockedPoliceFaction" exact="$legality.$BlockedPoliceFaction"/>
        <set_value name="$result.$ShouldAbort" exact="true"/>
        <set_value name="global.$GT_Ships.{$ship}.$RouteIllegalAbort" exact="table[
          $Reason = 'route_illegal_pre_transit',
          $NextSector = $checkSector,
          $NextSectorName = if $checkSector? then @$checkSector.knownname else 'Unknown',
          $Ware = $legality.$BlockedWare,
          $WareName = if $legality.$BlockedWare != null then @$legality.$BlockedWare.name else 'Unknown',
          $ActiveWare = if $policy.$Ware? then $policy.$Ware else null,
          $ActiveWareName = if $policy.$Ware != null then @$policy.$Ware.name else 'none',
          $PoliceFaction = $legality.$BlockedPoliceFaction,
          $PoliceFactionName = if $legality.$BlockedPoliceFaction != null then @$legality.$BlockedPoliceFaction.knownname else 'Unknown',
          $PoliceFactionId = if $legality.$BlockedPoliceFaction? and $legality.$BlockedPoliceFaction.id? then $legality.$BlockedPoliceFaction.id else 'none',
          $Timestamp = player.age
        ]"/>
        <do_if value="$policy.$Ware? and $legality.$BlockedWare? and $legality.$BlockedWare == $policy.$Ware and $policy.$HomeSector? and $policy.$BuySector? and $policy.$SellSector?">
          <run_script name="'lib.gt.pathfinding'" result="$cacheBust">
            <param name="mode" value="'invalidate_route_legality_cache'"/>
            <param name="ship" value="$ship"/>
            <param name="homeSector" value="$policy.$HomeSector"/>
            <param name="buySector" value="$policy.$BuySector"/>
            <param name="sellSector" value="$policy.$SellSector"/>
            <param name="ware" value="$policy.$Ware"/>
            <param name="allowIllegal" value="true"/>
            <param name="enforceRouteIllegalLegality" value="true"/>
            <param name="startSector" value="if $policy.$RouteStart? then $policy.$RouteStart else (if $ship.sector? then $ship.sector else $policy.$BuySector)"/>
            <param name="blacklistGroup" value="if $blacklistGroup? then $blacklistGroup else blacklistgroup.civilian"/>
          </run_script>
          <set_value name="$result.$CacheInvalidated" exact="if $cacheBust.$Invalidated? then $cacheBust.$Invalidated else false"/>
        </do_if>
        <remove_value name="global.$GT_Ships.{$ship}.$ActiveTradePolicy"/>
        <do_if value="@global.$GT_GlobalSettings.$Debug.$EnableDebugLogging">
          <debug_text text="'[GT-RouteEnforce] BLOCK ' + $ship.idcode + ': next=' + @$checkSector.knownname + ', ware=' + (if $legality.$BlockedWare != null then @$legality.$BlockedWare.name else 'null') + ', police=' + (if $legality.$BlockedPoliceFaction != null then @$legality.$BlockedPoliceFaction.id else 'null') + ', cacheBust=' + $result.$CacheInvalidated" chance="100"/>
        </do_if>
        <wait exact="1ms" comment="Cooperative yield before return (wake-loop guard)"/>
        <return value="$result"/>
      </do_if>

      <!-- ================================================================= -->
      <!-- MODE: route_legality                                              -->
      <!-- ================================================================= -->
      <do_if value="$mode == 'route_legality'">
        <set_value name="$result" exact="table[
          $IsLegal = true,
          $Reason = 'skipped',
          $BlockedSector = null,
          $BlockedPoliceFaction = null,
          $RouteSectors = [],
          $RouteSignature = '',
          $BlacklistEpoch = if global.$GT_BlacklistEpoch? then global.$GT_BlacklistEpoch else 0
        ]"/>

        <!-- Route legality filtering only applies when illegal wares are allowed and the feature is enabled. -->
        <do_if value="not $allowIllegal or not $enforceRouteIllegalLegality">
          <do_if value="@global.$GT_GlobalSettings.$Debug.$EnableDebugLogging">
            <debug_text text="'[GT-PATH] route_legality skipped: allowIllegal=' + $allowIllegal + ', filter=' + $enforceRouteIllegalLegality" chance="100"/>
          </do_if>
          <wait exact="1ms" comment="Cooperative yield before immediate return (wake-loop guard)"/>
          <return value="$result"/>
        </do_if>

        <!-- Contraband already in the hold counts toward the route check (multitrade parity with the
             runtime guard in move.generic, which walks the whole cargo list). Without this a legal
             ware could be routed through a sector where cargo from an earlier leg is illegal, and the
             ship would abort mid-flight instead of never planning that route.
             Cheap boolean only - the ware list is rebuilt after the path build, since locals do not
             survive the chunked jump-path yields. -->
        <set_value name="$holdHasContraband" exact="false"/>
        <do_if value="$ship != null">
          <set_value name="$shipCargoList" exact="$ship.cargo.list"/>
          <do_all exact="$shipCargoList.count" counter="$rlcc">
            <do_if value="$shipCargoList.{$rlcc}? and @$shipCargoList.{$rlcc}.illegal gt 0">
              <set_value name="$holdHasContraband" exact="true"/>
              <break/>
            </do_if>
          </do_all>
        </do_if>

        <!-- Legal wares cannot trigger route-illegal blocks; skip expensive jump-path build (parity
             with GT_CheckRouteLegality). Only valid while the hold carries no contraband of its own. -->
        <do_if value="(not $ware? or @$ware.illegal le 0) and not $holdHasContraband">
          <do_if value="@global.$GT_GlobalSettings.$Debug.$EnableDebugLogging">
            <debug_text text="'[GT-PATH] route_legality skipped: legal ware (' + (if $ware? then @$ware.id else 'null') + ')'" chance="100"/>
          </do_if>
          <wait exact="1ms" comment="Cooperative yield before immediate return (wake-loop guard)"/>
          <return value="$result"/>
        </do_if>

        <!-- Defensive guard: if required context is missing, fail closed for this candidate. -->
        <do_if value="$ship == null or $buySector == null or $sellSector == null or $ware == null">
          <set_value name="$result.$IsLegal" exact="false"/>
          <set_value name="$result.$Reason" exact="'invalid_context'"/>
          <do_if value="@global.$GT_GlobalSettings.$Debug.$EnableDebugLogging">
            <debug_text text="'[GT-PATH] route_legality invalid_context: ship=' + $ship + ', buy=' + $buySector + ', sell=' + $sellSector + ', ware=' + $ware" chance="100"/>
          </do_if>
          <wait exact="1ms" comment="Cooperative yield before immediate return (wake-loop guard)"/>
          <return value="$result"/>
        </do_if>

        <set_value name="$shipOwner" exact="$ship.owner"/>
        <do_if value="$shipOwner == null">
          <set_value name="$result.$IsLegal" exact="false"/>
          <set_value name="$result.$Reason" exact="'invalid_owner'"/>
          <do_if value="@global.$GT_GlobalSettings.$Debug.$EnableDebugLogging">
            <debug_text text="'[GT-PATH] route_legality invalid_owner for ship=' + $ship" chance="100"/>
          </do_if>
          <wait exact="1ms" comment="Cooperative yield before immediate return (wake-loop guard)"/>
          <return value="$result"/>
        </do_if>

        <set_value name="$routeStart" exact="$startSector"/>
        <do_if value="$routeStart == null and $ship.sector?">
          <set_value name="$routeStart" exact="$ship.sector"/>
        </do_if>
        <do_if value="$routeStart == null">
          <set_value name="$routeStart" exact="$buySector"/>
        </do_if>

        <set_value name="$resolvedBlacklistGroup" exact="$blacklistGroup"/>
        <do_if value="$resolvedBlacklistGroup == null">
          <set_value name="$resolvedBlacklistGroup" exact="blacklistgroup.civilian"/>
        </do_if>

        <!-- Phase B (R2): align highway flag with vanilla DockAndWait / move.generic -->
        <set_value name="$useLocalHighways" exact="true"/>
        <do_if value="$ship? and ($ship.isclass.ship_l or $ship.isclass.ship_xl)">
          <set_value name="$useLocalHighways" exact="false"/>
        </do_if>

        <!-- Build route sectors: ship/start -> buy -> sell -->
        <set_value name="$routeSectors" exact="[]"/>
        <set_value name="$lastRouteSector" exact="null"/>
        <append_to_list name="$routeSectors" exact="$routeStart"/>
        <set_value name="$lastRouteSector" exact="$routeStart"/>

        <set_value name="$pathToBuy" exact="[]"/>
        <do_if value="$routeStart != $buySector">
          <get_jump_path component="$pathToBuy" multiple="true" uselocalhighways="$useLocalHighways" useblacklist="$resolvedBlacklistGroup" refobject="$ship" useknownpath="$ship.isplayerowned">
            <start object="$ship"/>
            <end object="$buySector"/>
          </get_jump_path>
        </do_if>
        <set_value name="this.$rlPathToBuyList" exact="$pathToBuy"/>
        <set_value name="this.$rlRouteSectors" exact="$routeSectors"/>
        <set_value name="this.$rlLastRouteSector" exact="$lastRouteSector"/>
        <set_value name="this.$rlPathToBuyIdx" exact="1"/>
        <do_while value="this.$rlPathToBuyIdx le this.$rlPathToBuyList.count">
          <set_value name="$pathToBuyChunkEnd" exact="[this.$rlPathToBuyIdx + 24, this.$rlPathToBuyList.count].min"/>
          <do_while value="this.$rlPathToBuyIdx le $pathToBuyChunkEnd">
            <set_value name="$pathComponent" exact="this.$rlPathToBuyList.{this.$rlPathToBuyIdx}"/>
            <do_if value="$pathComponent? and @$pathComponent.sector != null">
              <set_value name="$pathSector" exact="@$pathComponent.sector"/>
              <do_if value="this.$rlLastRouteSector == null or $pathSector != this.$rlLastRouteSector">
                <append_to_list name="this.$rlRouteSectors" exact="$pathSector"/>
                <set_value name="this.$rlLastRouteSector" exact="$pathSector"/>
              </do_if>
            </do_if>
            <set_value name="this.$rlPathToBuyIdx" exact="this.$rlPathToBuyIdx + 1"/>
          </do_while>
          <do_if value="this.$rlPathToBuyIdx le this.$rlPathToBuyList.count">
            <wait exact="1ms"/>
          </do_if>
        </do_while>
        <set_value name="$routeSectors" exact="this.$rlRouteSectors"/>
        <set_value name="$lastRouteSector" exact="this.$rlLastRouteSector"/>
        <remove_value name="this.$rlPathToBuyList"/>
        <remove_value name="this.$rlRouteSectors"/>
        <remove_value name="this.$rlLastRouteSector"/>
        <remove_value name="this.$rlPathToBuyIdx"/>
        <do_if value="$lastRouteSector != $buySector">
          <append_to_list name="$routeSectors" exact="$buySector"/>
          <set_value name="$lastRouteSector" exact="$buySector"/>
        </do_if>

        <set_value name="$pathToSell" exact="[]"/>
        <do_if value="$buySector != $sellSector">
          <get_jump_path component="$pathToSell" multiple="true" uselocalhighways="$useLocalHighways" useblacklist="$resolvedBlacklistGroup" refobject="$ship" useknownpath="$ship.isplayerowned">
            <start object="$buySector"/>
            <end object="$sellSector"/>
          </get_jump_path>
        </do_if>
        <set_value name="this.$rlPathToSellList" exact="$pathToSell"/>
        <set_value name="this.$rlRouteSectors" exact="$routeSectors"/>
        <set_value name="this.$rlLastRouteSector" exact="$lastRouteSector"/>
        <set_value name="this.$rlPathToSellIdx" exact="1"/>
        <do_while value="this.$rlPathToSellIdx le this.$rlPathToSellList.count">
          <set_value name="$pathToSellChunkEnd" exact="[this.$rlPathToSellIdx + 24, this.$rlPathToSellList.count].min"/>
          <do_while value="this.$rlPathToSellIdx le $pathToSellChunkEnd">
            <set_value name="$pathComponent2" exact="this.$rlPathToSellList.{this.$rlPathToSellIdx}"/>
            <do_if value="$pathComponent2? and @$pathComponent2.sector != null">
              <set_value name="$pathSector2" exact="@$pathComponent2.sector"/>
              <do_if value="this.$rlLastRouteSector == null or $pathSector2 != this.$rlLastRouteSector">
                <append_to_list name="this.$rlRouteSectors" exact="$pathSector2"/>
                <set_value name="this.$rlLastRouteSector" exact="$pathSector2"/>
              </do_if>
            </do_if>
            <set_value name="this.$rlPathToSellIdx" exact="this.$rlPathToSellIdx + 1"/>
          </do_while>
          <do_if value="this.$rlPathToSellIdx le this.$rlPathToSellList.count">
            <wait exact="1ms"/>
          </do_if>
        </do_while>
        <set_value name="$routeSectors" exact="this.$rlRouteSectors"/>
        <set_value name="$lastRouteSector" exact="this.$rlLastRouteSector"/>
        <remove_value name="this.$rlPathToSellList"/>
        <remove_value name="this.$rlRouteSectors"/>
        <remove_value name="this.$rlLastRouteSector"/>
        <remove_value name="this.$rlPathToSellIdx"/>
        <do_if value="$lastRouteSector != $sellSector">
          <append_to_list name="$routeSectors" exact="$sellSector"/>
        </do_if>

        <!-- Build topology signature (path-aware cache key component). -->
        <set_value name="$routeSignature" exact="'E' + (if global.$GT_BlacklistEpoch? then global.$GT_BlacklistEpoch else 0) + '_'" />
        <set_value name="this.$rlRouteSectorsForSig" exact="$routeSectors"/>
        <set_value name="this.$rlRouteSignature" exact="$routeSignature"/>
        <set_value name="this.$rlRouteSigIdx" exact="1"/>
        <do_while value="this.$rlRouteSigIdx le this.$rlRouteSectorsForSig.count">
          <set_value name="$routeSigChunkEnd" exact="[this.$rlRouteSigIdx + 24, this.$rlRouteSectorsForSig.count].min"/>
          <do_while value="this.$rlRouteSigIdx le $routeSigChunkEnd">
            <set_value name="$rsec" exact="this.$rlRouteSectorsForSig.{this.$rlRouteSigIdx}"/>
            <!-- Cache key safety: use idcode-only tokens (no localized names/spaces/symbols). -->
            <set_value name="$rsecKey" exact="'S' + this.$rlRouteSigIdx" />
            <do_if value="$rsec? and $rsec.idcode?">
              <set_value name="$rsecKey" exact="$rsec.idcode"/>
            </do_if>
            <set_value name="this.$rlRouteSignature" exact="this.$rlRouteSignature + (if this.$rlRouteSigIdx gt 1 then '_' else '') + $rsecKey"/>
            <set_value name="this.$rlRouteSigIdx" exact="this.$rlRouteSigIdx + 1"/>
          </do_while>
          <do_if value="this.$rlRouteSigIdx le this.$rlRouteSectorsForSig.count">
            <wait exact="1ms"/>
          </do_if>
        </do_while>
        <set_value name="$routeSignature" exact="this.$rlRouteSignature"/>
        <remove_value name="this.$rlRouteSectorsForSig"/>
        <remove_value name="this.$rlRouteSignature"/>
        <remove_value name="this.$rlRouteSigIdx"/>

        <!-- Wares that must clear every sector on the route: whatever contraband is already aboard,
             plus the candidate ware when it is contraband. Duplicates are harmless (same verdict).
             Rebuilt here rather than carried across the chunked path build above, and held on this.
             so it survives the legality loop's own yields. -->
        <set_value name="this.$rlCheckWares" exact="[]"/>
        <do_all exact="$ship.cargo.list.count" counter="$rlcw0">
          <do_if value="$ship.cargo.list.{$rlcw0}? and @$ship.cargo.list.{$rlcw0}.illegal gt 0">
            <append_to_list name="this.$rlCheckWares" exact="$ship.cargo.list.{$rlcw0}"/>
          </do_if>
        </do_all>
        <set_value name="this.$rlCargoDirty" exact="this.$rlCheckWares.count gt 0"/>
        <do_if value="$ware? and @$ware.illegal gt 0">
          <append_to_list name="this.$rlCheckWares" exact="$ware"/>
        </do_if>

        <!-- Cache lookup (path-aware key). -->
        <do_if value="not global.$GT_RouteLegalityCache?">
          <set_value name="global.$GT_RouteLegalityCache" exact="table[]"/>
        </do_if>
        <set_value name="$cachedEntry" exact="null"/>
        <do_if value="global.$GT_RouteLegalityCache.{$homeSector}? and global.$GT_RouteLegalityCache.{$homeSector}.{$buySector}? and global.$GT_RouteLegalityCache.{$homeSector}.{$buySector}.{$sellSector}? and global.$GT_RouteLegalityCache.{$homeSector}.{$buySector}.{$sellSector}.{$ware}? and global.$GT_RouteLegalityCache.{$homeSector}.{$buySector}.{$sellSector}.{$ware}.{$allowIllegal}? and global.$GT_RouteLegalityCache.{$homeSector}.{$buySector}.{$sellSector}.{$ware}.{$allowIllegal}.{$enforceRouteIllegalLegality}? and global.$GT_RouteLegalityCache.{$homeSector}.{$buySector}.{$sellSector}.{$ware}.{$allowIllegal}.{$enforceRouteIllegalLegality}.{$shipOwner}? and global.$GT_RouteLegalityCache.{$homeSector}.{$buySector}.{$sellSector}.{$ware}.{$allowIllegal}.{$enforceRouteIllegalLegality}.{$shipOwner}.{$resolvedBlacklistGroup}? and global.$GT_RouteLegalityCache.{$homeSector}.{$buySector}.{$sellSector}.{$ware}.{$allowIllegal}.{$enforceRouteIllegalLegality}.{$shipOwner}.{$resolvedBlacklistGroup}.{$routeStart}?">
          <set_value name="$routeEntryList" exact="global.$GT_RouteLegalityCache.{$homeSector}.{$buySector}.{$sellSector}.{$ware}.{$allowIllegal}.{$enforceRouteIllegalLegality}.{$shipOwner}.{$resolvedBlacklistGroup}.{$routeStart}"/>
          <do_if value="$routeEntryList? and $routeEntryList.count gt 0">
            <do_all exact="$routeEntryList.count" counter="$reli">
              <do_if value="$routeEntryList.{$reli}? and $routeEntryList.{$reli}.$RouteSignature? and $routeEntryList.{$reli}.$RouteSignature == $routeSignature">
                <set_value name="$cachedEntry" exact="$routeEntryList.{$reli}"/>
                <break/>
              </do_if>
            </do_all>
          </do_if>
        </do_if>
        <!-- Cargo-dependent verdicts are never served from (or written to) the shared cache: the key
             has no cargo component, so one loaded ship would poison the entry for every other ship
             on the same route. Per-run memos in tradematch still cover the hot path. -->
        <do_if value="not this.$rlCargoDirty and $cachedEntry? and $cachedEntry.$CheckedAt? and (player.age - $cachedEntry.$CheckedAt) le $routeCacheTtl">
          <set_value name="$result" exact="$cachedEntry"/>
          <set_value name="$result.$RouteSectors" exact="$routeSectors"/>
          <do_if value="@global.$GT_GlobalSettings.$Debug.$EnableDebugLogging">
            <debug_text text="'[GT-PATH] route_legality cache_hit: legal=' + $result.$IsLegal + ', reason=' + $result.$Reason + ', routeSectors=' + $routeSectors.count + ', sig=' + $routeSignature" chance="100"/>
          </do_if>
          <remove_value name="this.$rlCheckWares"/>
          <remove_value name="this.$rlCargoDirty"/>
          <wait exact="1ms" comment="Cooperative yield before immediate return (wake-loop guard)"/>
          <return value="$result"/>
        </do_if>

        <!-- Evaluate sector legality against police factions on the actual route. -->
        <set_value name="$isLegal" exact="true"/>
        <set_value name="$blockedSector" exact="null"/>
        <set_value name="$blockedPolice" exact="null"/>
        <set_value name="$reason" exact="'ok'"/>
        <set_value name="this.$rlRouteSectorsForLegality" exact="$routeSectors"/>
        <set_value name="this.$rlIsLegal" exact="$isLegal"/>
        <set_value name="this.$rlBlockedSector" exact="$blockedSector"/>
        <set_value name="this.$rlBlockedPolice" exact="$blockedPolice"/>
        <set_value name="this.$rlReason" exact="$reason"/>
        <set_value name="this.$rlLegalityIdx" exact="1"/>
        <do_while value="this.$rlLegalityIdx le this.$rlRouteSectorsForLegality.count and this.$rlIsLegal">
          <set_value name="$legalityChunkEnd" exact="[this.$rlLegalityIdx + 24, this.$rlRouteSectorsForLegality.count].min"/>
          <do_while value="this.$rlLegalityIdx le $legalityChunkEnd and this.$rlIsLegal">
            <set_value name="$checkSector" exact="this.$rlRouteSectorsForLegality.{this.$rlLegalityIdx}"/>
            <do_if value="$checkSector? and $checkSector.policefaction and ($checkSector.policefaction != $shipOwner)">
              <do_all exact="this.$rlCheckWares.count" counter="$rlcw">
                <set_value name="$rlCheckWare" exact="this.$rlCheckWares.{$rlcw}"/>
                <do_if value="$rlCheckWare? and $rlCheckWare.illegalto.{$checkSector.policefaction}.{$shipOwner}">
                  <set_value name="this.$rlIsLegal" exact="false"/>
                  <set_value name="this.$rlBlockedSector" exact="$checkSector"/>
                  <set_value name="this.$rlBlockedPolice" exact="$checkSector.policefaction"/>
                  <set_value name="this.$rlReason" exact="'illegal_on_route'"/>
                  <break/>
                </do_if>
              </do_all>
            </do_if>
            <set_value name="this.$rlLegalityIdx" exact="this.$rlLegalityIdx + 1"/>
          </do_while>
          <do_if value="this.$rlLegalityIdx le this.$rlRouteSectorsForLegality.count and this.$rlIsLegal">
            <wait exact="1ms"/>
          </do_if>
        </do_while>
        <set_value name="$isLegal" exact="this.$rlIsLegal"/>
        <set_value name="$blockedSector" exact="this.$rlBlockedSector"/>
        <set_value name="$blockedPolice" exact="this.$rlBlockedPolice"/>
        <set_value name="$reason" exact="this.$rlReason"/>
        <set_value name="$cargoDirty" exact="this.$rlCargoDirty"/>
        <remove_value name="this.$rlCheckWares"/>
        <remove_value name="this.$rlCargoDirty"/>
        <remove_value name="this.$rlRouteSectorsForLegality"/>
        <remove_value name="this.$rlIsLegal"/>
        <remove_value name="this.$rlBlockedSector"/>
        <remove_value name="this.$rlBlockedPolice"/>
        <remove_value name="this.$rlReason"/>
        <remove_value name="this.$rlLegalityIdx"/>

        <set_value name="$result" exact="table[
          $IsLegal = $isLegal,
          $Reason = $reason,
          $BlockedSector = $blockedSector,
          $BlockedPoliceFaction = $blockedPolice,
          $RouteSectors = $routeSectors,
          $RouteSignature = $routeSignature,
          $BlacklistEpoch = if global.$GT_BlacklistEpoch? then global.$GT_BlacklistEpoch else 0,
          $CheckedAt = player.age
        ]"/>
        <do_if value="@global.$GT_GlobalSettings.$Debug.$EnableDebugLogging">
          <debug_text text="'[GT-PATH] route_legality evaluated: legal=' + $result.$IsLegal + ', reason=' + $result.$Reason + ', blockedSector=' + $result.$BlockedSector + ', blockedPolice=' + $result.$BlockedPoliceFaction + ', routeSectors=' + $routeSectors.count + ', sig=' + $routeSignature" chance="100"/>
        </do_if>

        <!-- Cargo-dependent verdict: return without storing. The cache key has no cargo component,
             so persisting this would apply one ship's hold to every other ship on the same route. -->
        <do_if value="$cargoDirty">
          <wait exact="1ms" comment="Cooperative yield before return (wake-loop guard)"/>
          <return value="$result"/>
        </do_if>

        <!-- Cache store -->
        <do_if value="not global.$GT_RouteLegalityCache.{$homeSector}?">
          <set_value name="global.$GT_RouteLegalityCache.{$homeSector}" exact="table[]"/>
        </do_if>
        <do_if value="not global.$GT_RouteLegalityCache.{$homeSector}.{$buySector}?">
          <set_value name="global.$GT_RouteLegalityCache.{$homeSector}.{$buySector}" exact="table[]"/>
        </do_if>
        <do_if value="not global.$GT_RouteLegalityCache.{$homeSector}.{$buySector}.{$sellSector}?">
          <set_value name="global.$GT_RouteLegalityCache.{$homeSector}.{$buySector}.{$sellSector}" exact="table[]"/>
        </do_if>
        <do_if value="not global.$GT_RouteLegalityCache.{$homeSector}.{$buySector}.{$sellSector}.{$ware}?">
          <set_value name="global.$GT_RouteLegalityCache.{$homeSector}.{$buySector}.{$sellSector}.{$ware}" exact="table[]"/>
        </do_if>
        <do_if value="not global.$GT_RouteLegalityCache.{$homeSector}.{$buySector}.{$sellSector}.{$ware}.{$allowIllegal}?">
          <set_value name="global.$GT_RouteLegalityCache.{$homeSector}.{$buySector}.{$sellSector}.{$ware}.{$allowIllegal}" exact="table[]"/>
        </do_if>
        <do_if value="not global.$GT_RouteLegalityCache.{$homeSector}.{$buySector}.{$sellSector}.{$ware}.{$allowIllegal}.{$enforceRouteIllegalLegality}?">
          <set_value name="global.$GT_RouteLegalityCache.{$homeSector}.{$buySector}.{$sellSector}.{$ware}.{$allowIllegal}.{$enforceRouteIllegalLegality}" exact="table[]"/>
        </do_if>
        <do_if value="not global.$GT_RouteLegalityCache.{$homeSector}.{$buySector}.{$sellSector}.{$ware}.{$allowIllegal}.{$enforceRouteIllegalLegality}.{$shipOwner}?">
          <set_value name="global.$GT_RouteLegalityCache.{$homeSector}.{$buySector}.{$sellSector}.{$ware}.{$allowIllegal}.{$enforceRouteIllegalLegality}.{$shipOwner}" exact="table[]"/>
        </do_if>
        <do_if value="not global.$GT_RouteLegalityCache.{$homeSector}.{$buySector}.{$sellSector}.{$ware}.{$allowIllegal}.{$enforceRouteIllegalLegality}.{$shipOwner}.{$resolvedBlacklistGroup}?">
          <set_value name="global.$GT_RouteLegalityCache.{$homeSector}.{$buySector}.{$sellSector}.{$ware}.{$allowIllegal}.{$enforceRouteIllegalLegality}.{$shipOwner}.{$resolvedBlacklistGroup}" exact="table[]"/>
        </do_if>
        <do_if value="not global.$GT_RouteLegalityCache.{$homeSector}.{$buySector}.{$sellSector}.{$ware}.{$allowIllegal}.{$enforceRouteIllegalLegality}.{$shipOwner}.{$resolvedBlacklistGroup}.{$routeStart}?">
          <set_value name="global.$GT_RouteLegalityCache.{$homeSector}.{$buySector}.{$sellSector}.{$ware}.{$allowIllegal}.{$enforceRouteIllegalLegality}.{$shipOwner}.{$resolvedBlacklistGroup}.{$routeStart}" exact="[]"/>
        </do_if>
        <set_value name="$routeEntryList" exact="global.$GT_RouteLegalityCache.{$homeSector}.{$buySector}.{$sellSector}.{$ware}.{$allowIllegal}.{$enforceRouteIllegalLegality}.{$shipOwner}.{$resolvedBlacklistGroup}.{$routeStart}"/>
        <!-- Drop entries from superseded blacklist epochs: the epoch is baked into $RouteSignature,
             so they can never match again and would otherwise accumulate in the savegame forever.
             Runs on store only, which is exactly the first access after an epoch bump. -->
        <do_if value="$routeEntryList? and $routeEntryList.count gt 0">
          <set_value name="$routeEntryKept" exact="[]"/>
          <do_all exact="$routeEntryList.count" counter="$reli0">
            <do_if value="$routeEntryList.{$reli0}? and $routeEntryList.{$reli0}.$BlacklistEpoch? and $routeEntryList.{$reli0}.$BlacklistEpoch == $result.$BlacklistEpoch">
              <append_to_list name="$routeEntryKept" exact="$routeEntryList.{$reli0}"/>
            </do_if>
          </do_all>
          <set_value name="$routeEntryList" exact="$routeEntryKept"/>
        </do_if>
        <set_value name="$routeEntryUpdated" exact="false"/>
        <do_if value="$routeEntryList? and $routeEntryList.count gt 0">
          <do_all exact="$routeEntryList.count" counter="$reli2">
            <do_if value="$routeEntryList.{$reli2}? and $routeEntryList.{$reli2}.$RouteSignature? and $routeEntryList.{$reli2}.$RouteSignature == $routeSignature">
              <set_value name="$routeEntryList.{$reli2}" exact="$result"/>
              <set_value name="$routeEntryUpdated" exact="true"/>
              <break/>
            </do_if>
          </do_all>
        </do_if>
        <do_if value="not $routeEntryUpdated">
          <append_to_list name="$routeEntryList" exact="$result"/>
        </do_if>
        <set_value name="global.$GT_RouteLegalityCache.{$homeSector}.{$buySector}.{$sellSector}.{$ware}.{$allowIllegal}.{$enforceRouteIllegalLegality}.{$shipOwner}.{$resolvedBlacklistGroup}.{$routeStart}" exact="$routeEntryList"/>

        <wait exact="1ms" comment="Cooperative yield before return (wake-loop guard)"/>
        <return value="$result"/>
      </do_if>

      <!-- ================================================================= -->
      <!-- MODE: spaces (legacy behavior)                                    -->
      <!-- ================================================================= -->
      <do_if value="$mode == 'spaces'">
      <set_value name="$refobject" exact="$homeSector"/>
      <do_if value="not $refobject? or not $refobject.exists">
        <set_value name="$refobject" exact="if $ship? and $ship.sector? then $ship.sector else null"/>
      </do_if>
      <do_if value="not $refobject? or not $refobject.exists">
        <do_if value="$ship? and @$ship.zone.issuperhighway">
          <set_value name="$refobject" exact="$ship.zone.destination.sector"/>
        </do_if>
      </do_if>
      <do_if value="not $refobject? or not $refobject.exists">
        <set_value name="$spaces" exact="[]"/>
        <set_value name="$result" exact="table[
          $BuySpaces = $spaces,
          $SellSpaces = $spaces,
          $ShipIsIsolated = true
        ]"/>
        <wait exact="1ms" comment="Cooperative yield before return (wake-loop guard)"/>
        <return value="$result"/>
      </do_if>

      <!-- Dual-radius: scan at max(buy,sell), then split BuySpaces (supplier) / SellSpaces (delivery).
           ignoreBlacklist=true (cache fill): neutral gatedistance only — per-ship BL is applied at
           cache read (lib.gt.cache.score_for_ship) from the reader's current sector.
           Blocked sectors are removed afterwards via filterBlacklistedSpaces (membership only). -->
      <set_value name="$buyLeg" exact="if $maxDistanceBuy? and $maxDistanceBuy gt 0 then $maxDistanceBuy else $maxDistance"/>
      <set_value name="$sellLeg" exact="if $maxDistanceSell? and $maxDistanceSell gt 0 then $maxDistanceSell else $maxDistance"/>
      <set_value name="$combinedMax" exact="[$buyLeg, $sellLeg, $maxDistance].max"/>
      <set_value name="$applySpacesBlacklist" exact="not ($ignoreBlacklist? and $ignoreBlacklist)"/>

      <!-- Always pass a real blacklistgroup: vanilla lib.find.sectors.inrange evaluates
           hasblacklist.{...}.{$blacklistgroup} even when applyblacklist=false, and {null} aborts.
           Neutral fill is controlled only by applyblacklist=false. -->
      <set_value name="$spacesBlacklistGroup" exact="if $blacklistGroup? and $blacklistGroup != null then $blacklistGroup else blacklistgroup.civilian"/>
      <set_value name="$mspBlGroup" exact="$spacesBlacklistGroup"/>
      <set_value name="$mspHasBlacklist" exact="$ship? and $ship.exists and ($ship.hasblacklist.{blacklisttype.sectoractivity}.{$mspBlGroup} or $ship.hasblacklist.{blacklisttype.sectortravel}.{$mspBlGroup})"/>
      <set_value name="$mspScoping" exact="$filterBlacklistedSpaces? and $filterBlacklistedSpaces and $mspHasBlacklist"/>

      <!-- Shared scope cache: the blacklist-scoped opportunity set depends only on home sector,
           radius and the blacklist, so the whole fleet reuses one enumeration instead of every ship
           re-walking the radius to reach the same answer. Skipped when an order whitelist is set
           (per-fleet input that does not belong in a home-keyed entry). TTL covers sectors the
           player discovers after the entry was built. -->
      <set_value name="$mspWlCount" exact="if $sectorWhitelist? and $sectorWhitelist != null then $sectorWhitelist.count else 0"/>
      <set_value name="$mspCacheable" exact="$mspScoping and not $applySpacesBlacklist and $mspWlCount == 0"/>
      <set_value name="$mspBlEpoch" exact="if global.$GT_BlacklistEpoch? then global.$GT_BlacklistEpoch else 0"/>
      <set_value name="$mspFromCache" exact="false"/>
      <do_if value="$mspCacheable">
        <do_if value="not global.$GT_SpacesScopeCache?">
          <set_value name="global.$GT_SpacesScopeCache" exact="table[]"/>
        </do_if>
        <do_if value="global.$GT_SpacesScopeCache.{$refobject}? and global.$GT_SpacesScopeCache.{$refobject}.{$combinedMax}? and global.$GT_SpacesScopeCache.{$refobject}.{$combinedMax}.{$mspBlGroup}?">
          <set_value name="$mspEntry" exact="global.$GT_SpacesScopeCache.{$refobject}.{$combinedMax}.{$mspBlGroup}"/>
          <do_if value="$mspEntry.$Epoch == $mspBlEpoch and $mspEntry.$Spaces? and (player.age - $mspEntry.$CachedAt) lt 60s">
            <set_value name="$spaces" exact="$mspEntry.$Spaces"/>
            <set_value name="$mspFromCache" exact="true"/>
          </do_if>
        </do_if>
      </do_if>

      <do_if value="not $mspFromCache">
        <run_script name="'lib.find.sectors.inrange'" result="$spaces">
          <param name="refobject" value="$refobject"/>
          <param name="mingatedistance" value="0"/>
          <param name="maxgatedistance" value="$combinedMax"/>
          <param name="applyblacklist" value="$applySpacesBlacklist"/>
          <param name="blacklistgroup" value="$spacesBlacklistGroup"/>
          <param name="returndistancetable" value="false"/>
          <param name="debugchance" value="$debugchance"/>
        </run_script>

        <do_if value="not $spaces?">
          <set_value name="$spaces" exact="[]"/>
        </do_if>

        <!-- Membership-only blacklist filter: drop sectors that are themselves blocked, but keep
             sectors the home cannot path to under the blacklist. Vanilla applyblacklist=true would
             also require a blacklist-aware home<->sector path, which removes allowed sectors sitting
             behind a blocked one - ships stranded on that side must still find trades there.
             Reachability stays a per-ship concern at validation. Ship/commander sector exemptions are
             applied after the cache so the stored list stays fleet-shareable. -->
        <do_if value="$mspScoping and $spaces.count gt 0">
          <create_list name="$mspAllowed"/>
          <do_all exact="$spaces.count" counter="$mspi">
            <set_value name="$mspSector" exact="$spaces.{$mspi}"/>
            <set_value name="$mspWhitelisted" exact="false"/>
            <do_if value="$mspWlCount gt 0">
              <do_all exact="$mspWlCount" counter="$mspw">
                <do_if value="$sectorWhitelist.{$mspw} == $mspSector">
                  <set_value name="$mspWhitelisted" exact="true"/>
                  <break/>
                </do_if>
              </do_all>
            </do_if>
            <do_if value="not $mspWhitelisted and ($mspSector.isblacklisted.{blacklisttype.sectoractivity}.{$mspBlGroup}.{$ship} or $mspSector.isblacklisted.{blacklisttype.sectortravel}.{$mspBlGroup}.{$ship})">
              <continue/>
            </do_if>
            <append_to_list name="$mspAllowed" exact="$mspSector"/>
          </do_all>
          <do_if value="@global.$GT_GlobalSettings.$Debug.$EnableDebugLogging and $mspAllowed.count != $spaces.count">
            <debug_text text="'[GT-Pathfinding] ' + $ship.idcode + ': blacklist-scoped live search spaces ' + $spaces.count + ' -> ' + $mspAllowed.count + ' sectors (home-disconnected allowed sectors kept)'" chance="100"/>
          </do_if>
          <set_value name="$spaces" exact="$mspAllowed"/>
        </do_if>

        <do_if value="$mspCacheable">
          <do_if value="not global.$GT_SpacesScopeCache.{$refobject}?">
            <set_value name="global.$GT_SpacesScopeCache.{$refobject}" exact="table[]"/>
          </do_if>
          <do_if value="not global.$GT_SpacesScopeCache.{$refobject}.{$combinedMax}?">
            <set_value name="global.$GT_SpacesScopeCache.{$refobject}.{$combinedMax}" exact="table[]"/>
          </do_if>
          <set_value name="global.$GT_SpacesScopeCache.{$refobject}.{$combinedMax}.{$mspBlGroup}" exact="table[
            $Epoch = $mspBlEpoch,
            $CachedAt = player.age,
            $Spaces = $spaces
          ]"/>
        </do_if>
      </do_if>

      <!-- Ship and commander sector stay in scope even when blocked: a stranded ship must be able to
           trade where it is. Applied per ship, outside the shared entry. -->
      <do_if value="$mspScoping and $ship? and $ship.exists">
        <!-- Copy first: $spaces may reference the shared cache entry, which must not be mutated. -->
        <create_list name="$mspFinal"/>
        <do_all exact="$spaces.count" counter="$mspCi">
          <append_to_list name="$mspFinal" exact="$spaces.{$mspCi}"/>
        </do_all>
        <set_value name="$mspShipSector" exact="if @$ship.zone.issuperhighway then @$ship.zone.destination.sector else @$ship.sector"/>
        <set_value name="$mspCommanderSector" exact="if $ship.commander? and $ship.commander.exists then @$ship.commander.sector else null"/>
        <do_all exact="2" counter="$mspEx">
          <set_value name="$mspExSector" exact="if $mspEx == 1 then $mspShipSector else $mspCommanderSector"/>
          <do_if value="not $mspExSector? or $mspExSector == null or not $mspExSector.exists">
            <continue/>
          </do_if>
          <set_value name="$mspExListed" exact="false"/>
          <do_all exact="$mspFinal.count" counter="$mspExj">
            <do_if value="$mspFinal.{$mspExj} == $mspExSector">
              <set_value name="$mspExListed" exact="true"/>
              <break/>
            </do_if>
          </do_all>
          <do_if value="not $mspExListed">
            <append_to_list name="$mspFinal" exact="$mspExSector"/>
          </do_if>
        </do_all>
        <set_value name="$spaces" exact="$mspFinal"/>
      </do_if>

      <!-- Filter out threat-blacklisted sectors (skip when neutral cache-fill opportunity set). -->
      <do_if value="$applySpacesBlacklist and $spaces.count gt 0 and global.$GT_ThreatIntelligence? and global.$GT_ThreatIntelligence.$BlacklistedSectors? and global.$GT_ThreatIntelligence.$BlacklistedSectors.count gt 0">
        <create_list name="$filteredSpaces"/>
        <do_all exact="$spaces.count" counter="$si">
          <set_value name="$sector" exact="$spaces.{$si}"/>
          <set_value name="$isThreatened" exact="false"/>
          <do_all exact="global.$GT_ThreatIntelligence.$BlacklistedSectors.count" counter="$bli">
            <do_if value="global.$GT_ThreatIntelligence.$BlacklistedSectors.{$bli} == $sector">
              <set_value name="$isThreatened" exact="true"/>
              <break/>
            </do_if>
          </do_all>
          <do_if value="not $isThreatened">
            <append_to_list name="$filteredSpaces" exact="$sector"/>
          </do_if>
        </do_all>
        <set_value name="$spaces" exact="$filteredSpaces"/>
      </do_if>

      <set_value name="$buySpaces" exact="$spaces"/>
      <set_value name="$sellSpaces" exact="$spaces"/>
      <do_if value="$buyLeg != $sellLeg and $spaces.count gt 0">
        <create_list name="$buySpaces"/>
        <create_list name="$sellSpaces"/>
        <do_all exact="$spaces.count" counter="$ssi">
          <set_value name="$sector" exact="$spaces.{$ssi}"/>
          <set_value name="$secDist" exact="$refobject.gatedistance.{$sector}"/>
          <do_if value="$secDist lt 0">
            <continue/>
          </do_if>
          <do_if value="$buyLeg le 0 or $secDist le $buyLeg">
            <append_to_list name="$buySpaces" exact="$sector"/>
          </do_if>
          <do_if value="$sellLeg le 0 or $secDist le $sellLeg">
            <append_to_list name="$sellSpaces" exact="$sector"/>
          </do_if>
        </do_all>
      </do_if>

      <set_value name="$result" exact="table[
        $BuySpaces = $buySpaces,
        $SellSpaces = $sellSpaces,
        $ShipIsIsolated = ($buySpaces.count == 0 and $sellSpaces.count == 0)
      ]"/>
      <wait exact="1ms" comment="Cooperative yield before return (wake-loop guard)"/>
      <return value="$result"/>
      </do_if>
    </actions>
  </attention>
</aiscript>
