<?xml version="1.0" encoding="utf-8"?>
<aiscript name="order.dock.pilotexchange" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="../../stock_game/X4 Foundations/libraries/aiscripts.xsd" version="1">
  <order id="DockAndPilotExchange" name="{77000, 10004}" description="{77000, 10104}" category="navigation" infinite="false">
    <params>
      <param name="destination" type="object" text="{1041, 10141}" comment="Station this ship docks at for the pilot exchange (ships dock independently; no shared rendezvous)">
        <input_param name="class" value="[class.station]"/>
      </param>
      <param name="partner" type="object" default="null" text="{1001, 1612}" comment="Other ship in the pilot exchange pair">
        <input_param name="class" value="[class.ship]"/>
      </param>
    </params>
    <requires>
      <match shiptype="shiptype.lasertower" negate="true"/>
    </requires>
    <location object="$destination" condition="@$destination.exists"/>
  </order>

  <interrupts>
    <handler ref="TargetInvalidHandler"/>
    <handler ref="AttackHandler"/>
  </interrupts>

  <init>
    <set_order_syncpoint_reached order="this.ship.order"/>
    <set_command command="command.dockat" param="$destination"/>
    <set_command_action commandaction="commandaction.waitingtodock" param="$destination"/>
    <debug_text text="'[GT PilotExchange] init ship=' + this.ship.idcode + ' destination=' + (if $destination? then $destination.idcode else 'NONE') + ' partner=' + (if $partner? then $partner.idcode else 'NONE')" chance="100"/>
    <do_if value="not $destination or not this.ship.pilot">
      <debug_text text="'[GT PilotExchange] init failed ship=' + this.ship.idcode + ' destination=' + (if $destination? then $destination.idcode else 'NONE') + ' hasPilot=' + (if this.ship.pilot? then 'yes' else 'no')" chance="100"/>
      <signal_objects object="player.galaxy" param="'GT_PilotExchange_OrderState'" param2="table[
        $Ship = this.ship,
        $ShipID = this.ship.idcode,
        $State = 'failed',
        $StationID = if $destination? then $destination.idcode else ''
      ]"/>
      <set_order_state order="this.ship.order" state="orderstate.critical"/>
      <return/>
    </do_if>
    <signal_objects object="player.galaxy" param="'GT_PilotExchange_OrderState'" param2="table[
      $Ship = this.ship,
      $ShipID = this.ship.idcode,
      $State = 'started',
      $StationID = $destination.idcode,
      $PartnerID = if $partner? then $partner.idcode else ''
    ]"/>
  </init>

  <attention min="unknown">
    <actions>
      <set_value name="$arrived" exact="false"/>

      <do_if value="(this.ship.dock and (this.ship.dock.container == $destination)) or (this.ship.parkedat and ((this.ship.parkedat == $destination) or this.ship.parkedat.hascontext.{$destination}))">
        <debug_text text="'[GT PilotExchange] already docked ship=' + this.ship.idcode + ' station=' + $destination.idcode" chance="100"/>
        <set_command_action commandaction="commandaction.standingby"/>
        <signal_objects object="player.galaxy" param="'GT_PilotExchange_OrderState'" param2="table[
          $Ship = this.ship,
          $ShipID = this.ship.idcode,
          $State = 'arrived',
          $StationID = $destination.idcode,
          $PartnerID = if $partner? then $partner.idcode else ''
        ]"/>
        <set_value name="$arrived" exact="true"/>
      </do_if>
      <do_if value="not $arrived">
        <debug_text text="'[GT PilotExchange] create DockAndWait ship=' + this.ship.idcode + ' station=' + $destination.idcode" chance="100"/>
        <set_command_action commandaction="commandaction.waitingtodock" param="$destination"/>
        <!-- Parent DockAndPilotExchange pauses while DockAndWait runs; flag handoff so on_abort does not tear down the PE session. -->
        <set_value name="this.$peDockHandoff" exact="true"/>
        <create_order name="$dockOrder" id="'DockAndWait'" object="this.ship" immediate="true">
          <param name="destination" value="$destination"/>
          <param name="building" value="false"/>
          <param name="trading" value="false"/>
          <param name="dockfollowers" value="$destination.isclass.station"/>
          <param name="internalorder" value="true"/>
          <param name="callerid" value="this.ship.order"/>
        </create_order>
        <set_value name="this.$peDockOrder" exact="$dockOrder"/>

        <wait>
          <interrupt>
            <conditions>
              <event_object_order_ready object="this.ship" order="$dockOrder"/>
            </conditions>
          </interrupt>
        </wait>

        <remove_value name="this.$peDockHandoff"/>
        <remove_value name="this.$peDockOrder"/>

        <do_if value="(this.ship.dock and (this.ship.dock.container == $destination)) or (this.ship.parkedat and ((this.ship.parkedat == $destination) or this.ship.parkedat.hascontext.{$destination}))">
          <debug_text text="'[GT PilotExchange] arrived ship=' + this.ship.idcode + ' station=' + $destination.idcode" chance="100"/>
          <set_command_action commandaction="commandaction.standingby"/>
          <signal_objects object="player.galaxy" param="'GT_PilotExchange_OrderState'" param2="table[
            $Ship = this.ship,
            $ShipID = this.ship.idcode,
            $State = 'arrived',
            $StationID = $destination.idcode,
            $PartnerID = if $partner? then $partner.idcode else ''
          ]"/>
          <set_value name="$arrived" exact="true"/>
        </do_if>
        <do_else>
          <debug_text text="'[GT PilotExchange] failed to dock ship=' + this.ship.idcode + ' station=' + $destination.idcode" chance="100"/>
          <signal_objects object="player.galaxy" param="'GT_PilotExchange_OrderState'" param2="table[
            $Ship = this.ship,
            $ShipID = this.ship.idcode,
            $State = 'failed',
            $StationID = $destination.idcode,
            $PartnerID = if $partner? then $partner.idcode else ''
          ]"/>
          <set_command_action commandaction="commandaction.orderfailed"/>
          <return/>
        </do_else>
      </do_if>

      <!-- Keep ship docked for exchange until explicit release signal.
           Failsafe timeout is aligned with MD swap-tracker timeout. -->
      <set_value name="$holdTime" exact="2h"/>
      <do_if value="$holdTime and typeof $holdTime != datatype.time">
        <set_value name="$holdTime" exact="$holdTime * 1s"/>
      </do_if>
      <!-- Match DockAndTrain: normalize to whole seconds before wait (engine 1/12s raw
           unit count otherwise makes multi-minute holds expire in ~1/12 of intended time). -->
      <set_value name="$peHoldWaitTime" exact="($holdTime / 1s) * 1s"/>
      <set_value name="this.$peHoldActive" exact="true"/>
      <wait min="$peHoldWaitTime" max="$peHoldWaitTime">
        <interrupt>
          <conditions>
            <event_object_signalled object="this.ship" param="'GT_PilotExchange_Release'"/>
          </conditions>
        </interrupt>
      </wait>
      <remove_value name="this.$peHoldActive"/>
      <debug_text text="'[GT PilotExchange] release/timeout ship=' + this.ship.idcode + ' station=' + $destination.idcode" chance="100"/>
      <signal_objects object="player.galaxy" param="'GT_PilotExchange_OrderState'" param2="table[
        $Ship = this.ship,
        $ShipID = this.ship.idcode,
        $State = 'released',
        $StationID = if $destination? then $destination.idcode else '',
        $PartnerID = if $partner? then $partner.idcode else ''
      ]"/>
    </actions>
  </attention>

  <on_abort>
    <!-- DockAndWait child pauses this script while the ship flies to the station.
         That engine pause fires on_abort; it is NOT a player cancel or failure. -->
    <set_value name="$isDockTransitHandoff" exact="false"/>
    <do_if value="this.$peDockHandoff? and this.$peDockHandoff">
      <set_value name="$isDockTransitHandoff" exact="true"/>
    </do_if>
    <do_if value="not $isDockTransitHandoff and this.$peDockOrder? and this.$peDockOrder.exists">
      <set_value name="$isDockTransitHandoff" exact="true"/>
    </do_if>
    <do_if value="not $isDockTransitHandoff and this.ship.order? and @this.ship.order.id == 'DockAndWait' and this.ship.order.$callerid? and @this.ship.order.$callerid.id == 'DockAndPilotExchange'">
      <set_value name="$isDockTransitHandoff" exact="true"/>
    </do_if>
    <do_if value="not $isDockTransitHandoff and this.ship.orders? and this.ship.orders.count gt 0">
      <do_all exact="this.ship.orders.count" counter="$peoi">
        <set_value name="$peOrder" exact="this.ship.orders.{$peoi}"/>
        <do_if value="@$peOrder.id == 'DockAndWait' and $peOrder.$callerid? and @$peOrder.$callerid.id == 'DockAndPilotExchange'">
          <set_value name="$isDockTransitHandoff" exact="true"/>
          <break/>
        </do_if>
      </do_all>
    </do_if>
    <do_if value="$isDockTransitHandoff">
      <debug_text text="'[GT PilotExchange] on_abort skipped (DockAndWait transit handoff - exchange still in progress) ship=' + this.ship.idcode" chance="100"/>
      <return/>
    </do_if>
    <do_if value="this.$peHoldActive? and this.$peHoldActive">
      <!-- Docked hold interrupted by a real abort (player cancelled the order, ship reassigned).
           Emit 'cancelled' so MD can release the partner; MD ignores this while a swap is actually
           in flight (awaiting settlement), so a transient settlement abort does not tear the pair down. -->
      <debug_text text="'[GT PilotExchange] on_abort during docked hold -> cancelled ship=' + this.ship.idcode" chance="100"/>
      <signal_objects object="player.galaxy" param="'GT_PilotExchange_OrderState'" param2="table[
        $Ship = this.ship,
        $ShipID = this.ship.idcode,
        $State = 'cancelled',
        $StationID = if $destination? then $destination.idcode else '',
        $PartnerID = if $partner? then $partner.idcode else ''
      ]"/>
      <return/>
    </do_if>

    <signal_objects object="player.galaxy" param="'GT_PilotExchange_OrderState'" param2="table[
      $Ship = this.ship,
      $ShipID = this.ship.idcode,
      $State = 'aborted',
      $StationID = if $destination? then $destination.idcode else '',
      $PartnerID = if $partner? then $partner.idcode else ''
    ]"/>
  </on_abort>
</aiscript>
